<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/delete_diary_menu"
        android:icon="@drawable/ic_popup_delete_menu"
        android:title="@string/popup_delete_menu"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/edit_diary_menu"
        android:icon="@drawable/ic_popup_edit_menu"
        android:title="@string/popup_edit_menu"
        app:showAsAction="ifRoom"/>
</menu>